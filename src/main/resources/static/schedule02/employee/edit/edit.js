$(
    function(){
        const originData=window.parent.originData;

        if(originData){
            switch(originData.type){
                case 0:
                break;
                case 1:
                break;
                default:
                break;
            }
        }
    }
);